#----------------------------------------------------------------

hdclub_passkey 		= ''
base_path			= '../media_library'

#----------------------------------------------------------------

import anidub, hdclub, nnmclub
import settings

settings = settings.Settings(base_path, hdclub_passkey=hdclub_passkey)
print settings

anidub.run(settings)
hdclub.run(settings)
nnmclub.run(settings)